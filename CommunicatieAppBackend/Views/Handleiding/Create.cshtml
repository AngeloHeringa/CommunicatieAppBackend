@model CommunicatieAppBackend.Models.HandleidingViewModel

@{
    ViewData["Title"] = "Create";
    String defaultdt = DateTime.Now.ToString("yyyy-MM-ddThh:mm");
}

<h1>Create</h1>

<h4>Handleiding</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form enctype="multipart/form-data" asp-controller="Handleiding" asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @* <input type="hidden" asp-for="HandleidingId" /> *@
            <div class="form-group">
                <label asp-for="Handleiding.Title" class="control-label"></label>
                <input asp-for="Handleiding.Title" class="form-control" />
                <span asp-validation-for="Handleiding.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Handleiding.Details" class="control-label"></label>
                <input asp-for="Handleiding.Details" class="form-control" />
                <span asp-validation-for="Handleiding.Details" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Document" class="control-label"></label>
                <input asp-for="Document" class="form-control">
                @* <div id="chooseFile"><input type="file" name="Foto" accept="image/*" /></div> *@
                <span asp-validation-for="Document" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Handleiding" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
